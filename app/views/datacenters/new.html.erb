<%= render 'shared/header' %>
<h2>New Datacenter</h2>
<%= form_with(model: @dc) do |form| %>
    <% if @dc.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(dc.errors.count, "error") %> prohibited this datacenter from being saved:</h2>
  
        <ul>
          <% @dc.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div>
      <%= form.label :name, style: "display: block" %>
      <%= form.text_field :name %>
    </div>
  
    <div>
      <%= form.label :location, style: "display: block" %>
      <%= form.text_area :location %>
    </div>
  
    <div>
      <%= form.submit %>
    </div>
  <% end %>
  